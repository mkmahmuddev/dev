import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Skills = () => {
    useTitle('Skills')

    return (
        <div>
            Skills
        </div>
    );
};

export default Skills;