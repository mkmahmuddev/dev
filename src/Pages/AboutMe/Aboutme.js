import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Aboutme = () => {
    useTitle('About Me')

    return (
        <div>
            About Me
        </div>
    );
};

export default Aboutme;